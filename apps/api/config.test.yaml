deployment:
  mode: local

server:
  port: 0
  host: 127.0.0.1

database:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  name: monkeys_memory_test
  logging: false

redis:
  host: localhost
  port: 6379
  password:

console:
  baseUrl: http://localhost:8080

cors:
  allowedOrigins:
    - http://localhost:8080
