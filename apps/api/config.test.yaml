deployment:
  mode: local

server:
  port: 0
  host: 127.0.0.1

database:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  name: monkeys_memory_test
  logging: false

redis:
  host: localhost
  port: 6379
  password:

auth:
  sessionSecret: test-session-secret-with-enough-length
  globalAdminSecret: test-global-admin-secret-with-enough-length
  adminMfaEncryptionKey: test-admin-mfa-encryption-key
  appBaseUrl: http://localhost:3000
  webBaseUrl: http://localhost:8080
  allowedOrigins:
    - http://localhost:8080
  cookieSecure: false
  cookieSameSite: lax
